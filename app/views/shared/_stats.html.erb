<% @user ||= current_user %>
<div class="clearfix">
  <strong><%= link_to pluralize(current_user.textposts.count, "textpost"), user_root_path(current_user) %></strong></br>
  <strong><%= link_to "#{@user.followed_users.count} following", following_user_path(@user) %></strong></br>
  <strong><%= link_to "#{@user.followers.count} followers", followers_user_path(@user) %></strong>
</div>