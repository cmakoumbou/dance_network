<p class="pull-left text_space"><%= link_to image_tag(current_user.avatar.url(:thumb), class: "round"), user_root_path(current_user) %></p>
<h2><strong><%= link_to current_user.username, user_root_path(current_user) %></strong></h2>
