<%= form_for [textpost, @comment] do |f| %>
	<div class="form-group">
		<%= f.text_area :content, placeholder: "Compose new comment....", class: "form-control special_textarea2", :rows => 3 %>
	</div>
	<%= f.submit "Post comment", class: "btn btn-default pull-right" %>
<% end %>