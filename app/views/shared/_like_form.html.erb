 <% if current_user.liked? textpost %>
    <strong><%= link_to "Unlike", unlike_textpost_path(textpost), method: :put %></strong>
 <% else %>
 	<strong><%= link_to "Like", like_textpost_path(textpost), method: :put %></strong>	
 <% end %>